// sidebar is only used on medium to large screens. But hidden on mobile screens
export default function Sidebar() {
    return (
        <section className="hidden">
            sidebar
        </section>
    )
}
